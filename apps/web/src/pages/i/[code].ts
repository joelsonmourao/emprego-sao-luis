import type { APIRoute } from "astro"; import { createDatabase, jobs } from "@es/db"; import { eq } from "drizzle-orm";
export const GET: APIRoute = async ({ params, url, redirect }) => { const code = (params.code ?? "").toUpperCase(); if (!/^ES-\d{6}$/.test(code) || !process.env.DATABASE_URL) return redirect("/vagas", 302); const connection = createDatabase(process.env.DATABASE_URL); try { const [job] = await connection.db.select({ slug: jobs.slug }).from(jobs).where(eq(jobs.publicCode, code)).limit(1); if (!job) return redirect("/vagas", 302); const destination = new URL(`/vagas/${job.slug}`, url.origin); for (const [key, value] of url.searchParams) if (key.startsWith("utm_")) destination.searchParams.set(key, value); if (!destination.searchParams.has("utm_source")) destination.searchParams.set("utm_source", "shortlink"); return redirect(destination.pathname + destination.search, 302); } finally { await connection.close(); } };
