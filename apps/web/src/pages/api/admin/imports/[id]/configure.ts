import type { APIRoute } from "astro";
import { auditLogs, createDatabase, importBatches, importMappingTemplates } from "@es/db";
import { importModeSchema } from "@es/shared";
import { eq } from "drizzle-orm";
import { z } from "zod";
import { can } from "../../../../../lib/auth";
import { createImportQueue } from "../../../../../lib/queue";
const fields = ["externalId","title","company","city","state","category","description","applyUrl","source","sourceUrl","expiresAt","employmentType","workplaceType","salaryMin","salaryMax"] as const;
const mappingSchema = z.record(z.enum(fields), z.string().min(1));
const settingsSchema = z.object({ storageKey: z.string(), sheets: z.array(z.object({ name: z.string(), headers: z.array(z.string()), preview: z.array(z.record(z.string(), z.unknown())) })) });
export const POST: APIRoute = async ({ params, request, locals, redirect, clientAddress }) => { const auth=locals.auth!; if(!can(auth,"imports.manage"))return new Response("Proibido",{status:403}); if(!params.id||!process.env.DATABASE_URL)return new Response("Dados inválidos",{status:400}); const form=await request.formData(); const mode=importModeSchema.exclude(["DRY_RUN"]).or(z.literal("DRY_RUN")).safeParse(form.get("mode")); const sheetName=z.string().min(1).safeParse(form.get("sheetName")); const duplicateStrategy=z.enum(["IGNORE","UPDATE","CREATE_NEW"]).safeParse(form.get("duplicateStrategy")); const mapping=mappingSchema.safeParse(Object.fromEntries(fields.map((field)=>[field,form.get(`map_${field}`)]).filter((entry):entry is [string,string]=>typeof entry[1]==="string"&&entry[1]!==""))); if(!mode.success||!sheetName.success||!duplicateStrategy.success||!mapping.success)return new Response("Configuração inválida",{status:400}); const connection=createDatabase(process.env.DATABASE_URL); try{const[batch]=await connection.db.select().from(importBatches).where(eq(importBatches.id,params.id)).limit(1); const current=settingsSchema.safeParse(batch?.settings); if(!batch||!current.success||!current.data.sheets.some((sheet)=>sheet.name===sheetName.data))return new Response("Lote indisponível",{status:409}); const settings={...current.data,stage:"QUEUED",mode:mode.data,sheetName:sheetName.data,mapping:mapping.data,duplicateStrategy:duplicateStrategy.data}; await connection.db.transaction(async(tx)=>{await tx.update(importBatches).set({settings,status:"PENDING",updatedAt:new Date()}).where(eq(importBatches.id,batch.id)); const templateName=String(form.get("templateName")??"").trim(); if(templateName)await tx.insert(importMappingTemplates).values({name:templateName,mapping:mapping.data,sheetName:sheetName.data,createdBy:auth.id}).onConflictDoUpdate({target:importMappingTemplates.name,set:{mapping:mapping.data,sheetName:sheetName.data,updatedAt:new Date()}}); await tx.insert(auditLogs).values({actorId:auth.id,action:"CONFIGURE_IMPORT",entityType:"IMPORT_BATCH",entityId:batch.id,after:{sheetName:sheetName.data,mapping:mapping.data,duplicateStrategy:duplicateStrategy.data,ip:clientAddress},origin:"ADMIN"});}); const queue=createImportQueue();try{await queue.add("process-job-file",{batchId:batch.id,storageKey:current.data.storageKey,mode:mode.data,sheetName:sheetName.data,mapping:mapping.data,duplicateStrategy:duplicateStrategy.data},{jobId:`${batch.id}-${Date.now()}`,attempts:5,backoff:{type:"exponential",delay:5000}});}finally{await queue.close();}return redirect(`/admin/importacao?batch=${batch.id}`,303);}finally{await connection.close();}};
