import { createHash } from "node:crypto"; import type { APIRoute } from "astro"; import { jobDraftSchema } from "@es/shared"; import { auditLogs, createDatabase, jobRevisions, jobs } from "@es/db"; import { and, eq } from "drizzle-orm"; import { z } from "zod"; import { can } from "../../../../../lib/auth";
const optional=(value:FormDataEntryValue|null)=>typeof value==="string"&&value.trim()?value.trim():undefined;
export const POST:APIRoute=async({params,request,locals,redirect,clientAddress})=>{const auth=locals.auth!;if(!can(auth,"jobs.create"))return new Response("Proibido",{status:403});const form=await request.formData();const version=z.coerce.number().int().positive().safeParse(form.get("version"));const parsed=jobDraftSchema.safeParse({...Object.fromEntries(form),categoryId:optional(form.get("categoryId")),sourceUrl:optional(form.get("sourceUrl")),salaryMin:optional(form.get("salaryMin")),salaryMax:optional(form.get("salaryMax"))});if(!params.id||!version.success||!parsed.success||!process.env.DATABASE_URL)return Response.json({error:"Dados inválidos",details:parsed.success?undefined:parsed.error.issues},{status:400});const connection=createDatabase(process.env.DATABASE_URL);try{await connection.db.transaction(async(tx)=>{const[before]=await tx.select().from(jobs).where(eq(jobs.id,params.id!)).limit(1);if(!before)throw new Error("Vaga não encontrada.");const[after]=await tx.update(jobs).set({...parsed.data,categoryId:parsed.data.categoryId??null,sourceUrl:parsed.data.sourceUrl??null,salaryMin:parsed.data.salaryMin?.toString(),salaryMax:parsed.data.salaryMax?.toString(),salaryVisible:parsed.data.salaryMin!==undefined||parsed.data.salaryMax!==undefined,duplicateHash:createHash("sha256").update(`${parsed.data.normalizedTitle}|${parsed.data.companyId}|${parsed.data.cityId}`).digest("hex"),version:before.version+1,updatedAt:new Date()}).where(and(eq(jobs.id,before.id),eq(jobs.version,version.data))).returning();if(!after)throw new Error("Conflito de edição. Recarregue a página.");await tx.insert(jobRevisions).values({jobId:before.id,version:before.version,snapshot:before,actorId:auth.id});await tx.insert(auditLogs).values({actorId:auth.id,action:"UPDATE",entityType:"JOB",entityId:before.id,before,after:{record:after,ip:clientAddress,userAgent:request.headers.get("user-agent")},origin:"ADMIN"});});return redirect(`/admin/vagas/${params.id}/editar?saved=1`,303);}finally{await connection.close();}};
