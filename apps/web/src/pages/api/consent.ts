import type { APIRoute } from "astro"; export const POST: APIRoute = async ({ request, cookies, redirect }) => { const form = await request.formData(); const choice = form.get("choice") === "yes" ? "yes" : "no"; cookies.set("es_consent_ads", choice, { path: "/", sameSite: "lax", secure: import.meta.env.PROD, maxAge: 60 * 60 * 24 * 180 }); return redirect(request.headers.get("referer") ?? "/", 303); };
