import type { APIRoute } from "astro"; import { escapeXml } from "@es/seo"; import { listPublishedArticles } from "../lib/articles";
export const GET: APIRoute = async ({ site }) => { const items = (await listPublishedArticles(1000, new Date(Date.now() - 2 * 86400000))).filter(({ article }) => article.type === "NEWS"); const body = `<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:news="http://www.google.com/schemas/sitemap-news/0.9">${items.map(({ article }) => `<url><loc>${escapeXml(new URL(`/blog/${article.slug}`, site).toString())}</loc><news:news><news:publication><news:name>Empregos São Luís</news:name><news:language>pt</news:language></news:publication><news:publication_date>${article.publishedAt?.toISOString()}</news:publication_date><news:title>${escapeXml(article.title)}</news:title></news:news></url>`).join("")}</urlset>`; return new Response(body, { headers: { "content-type": "application/xml; charset=utf-8" } }); };
