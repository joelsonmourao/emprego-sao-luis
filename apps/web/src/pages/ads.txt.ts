import type { APIRoute } from "astro"; export const GET: APIRoute = () => { const publisher = import.meta.env.PUBLIC_ADSENSE_PUBLISHER_ID as string | undefined; const body = publisher && /^pub-\d+$/.test(publisher) ? `google.com, ${publisher}, DIRECT, f08c47fec0942fa0\n` : "# AdSense ainda não ativado.\n"; return new Response(body, { headers: { "content-type": "text/plain; charset=utf-8" } }); };
