import { and, eq, lte } from "drizzle-orm"; import { createDatabase, indexingEvents, jobs } from "@es/db";
export async function publishScheduledJobs(){if(!process.env.DATABASE_URL)throw new Error("DATABASE_URL não configurada.");const connection=createDatabase(process.env.DATABASE_URL);try{return await connection.db.transaction(async(tx)=>{const published=await tx.update(jobs).set({publicationStatus:"PUBLISHED",publishedAt:new Date(),updatedAt:new Date()}).where(and(eq(jobs.publicationStatus,"SCHEDULED"),lte(jobs.scheduledAt,new Date()))).returning({id:jobs.id,slug:jobs.slug});for(const job of published){const url=new URL(`/vagas/${job.slug}`,process.env.SITE_URL??"https://empregossaoluis.com.br").toString();await tx.insert(indexingEvents).values([{dedupeKey:`scheduled:${job.id}:google`,jobId:job.id,provider:"GOOGLE",url,notificationType:"URL_UPDATED"},{dedupeKey:`scheduled:${job.id}:indexnow`,jobId:job.id,provider:"INDEXNOW",url,notificationType:"URL_UPDATED"}]).onConflictDoNothing();}return{published:published.length};});}finally{await connection.close();}}
